# Public browser configuration. Copy to .env.local for local development.
VITE_SUPABASE_URL=https://jkhxtuwqmmdetjqymzso.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# Set to 1 to disable the default HTTP ZH→EN adapter (Admin then leaves EN empty for pick() fallback).
# VITE_DISABLE_HTTP_TRANSLATION=1

# Never expose a service-role key through VITE_* variables or commit it to this repository.

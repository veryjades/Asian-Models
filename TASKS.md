# Asian Stars Agency — Task Board

**Current phase:** Phase 2 — Application Foundation
**Last updated:** 2026-09-12

## Master acceptance checklist

All product and release requirements are tracked in [`docs/PROJECT_CHECKLIST.md`](docs/PROJECT_CHECKLIST.md).
The checklist is the required cross-session memory for backend content, model
data, JAgent/RAG, deployment, and QA; do not start a later item without
updating the prior item's status and evidence.

## Phase 0 — COMPLETE

All Phase 0 tasks below are complete. PR #2 merged into `main` as `875f9eb`.

## Phase 0 tasks

| ID     | Task                                                              | Owner | Status   | Dependencies | Evidence / next action                                                                                                                                                                                                                                                                    |
| ------ | ----------------------------------------------------------------- | ----- | -------- | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P0-001 | Capture the current branch, commit, remotes, and worktree state.  | Codex | Complete | None         | `feature/baseline` at `302a318`; working tree was clean before control-document changes; `origin` is `veryjades/Asian-Models`.                                                                                                                                                            |
| P0-002 | Establish repository control documents and agent operating rules. | Codex | Complete | P0-001       | `DECISIONS.md`, `DEVELOPMENT_PLAN.md`, `TASKS.md`, `docs/architecture/README.md`, and `AGENTS.md` now define the shared control system.                                                                                                                                                   |
| P0-003 | Establish a reproducible package-manager/toolchain path.          | Codex | Complete | D-006        | Historical Phase 0 evidence: Bun was unavailable during the original baseline, so npm was used only for verification and no lockfile changed. Bun 1.3.14 is now available; B-001 records the current state. CI still requires an explicit Bun version pin.                            |
| P0-004 | Record lint baseline and bound failures.                          | Codex | Complete | None         | Initial lint had 46 Prettier errors in 17 existing files and no non-Prettier errors. Applied the repository Prettier configuration to those files. `npm run lint` now exits 0 with 9 existing React-refresh warnings and 0 errors.                                                        |
| P0-005 | Record production build baseline.                                 | Codex | Complete | P0-004       | `npm run build` completed successfully on 2026-08-10 after the baseline formatting changes; it emitted non-fatal Vite/Nitro warnings.                                                                                                                                                     |
| P0-006 | Establish automated-test baseline.                                | Codex | Complete | None         | No test files, test runner dependency, or `test` script exists. Minimum recommended future strategy: first add unit tests for pure content/repository/i18n behavior, then add route/workflow integration tests only when their Phase 1 acceptance criteria exist. No framework was added. |
| P0-007 | Open the control-protocol draft pull request.                     | Codex | Complete | B-003        | PR [#2](https://github.com/veryjades/Asian-Models/pull/2) merged from `feature/baseline` into `main` as `875f9eb`.                                                                                                                                                                        |

## Decision blockers

| ID    | Context                                                                                                         | Options                                                                                                                                   | Impact                                                                              | Proposed owner                            | Status               |
| ----- | --------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ----------------------------------------- | -------------------- |
| B-001 | This environment originally lacked Bun even though the repository's canonical workflow is Bun.                  | Bun 1.3.14 is now installed locally; CI still needs an explicit version pin.                                                              | `bun install --frozen-lockfile` now succeeds locally.                               | Repository owner / environment maintainer | Resolved             |
| B-002 | There is no automated test command or runner.                                                                   | Baseline absence and minimum strategy have been documented in P0-006; select/implement a framework only when a later phase authorizes it. | Future regression confidence, not the current Phase 0 baseline gate.                | Tech lead / product owner                 | Resolved for Phase 0 |
| B-003 | GitHub PR creation initially returned 403 through the connector.                                                | CLI fallback created draft PR [#2](https://github.com/veryjades/Asian-Models/pull/2).                                                     | Required review/merge path is now available.                                        | Repository owner / reviewer               | Resolved             |
| B-004 | `bunx tsc --noEmit` reported 10 strict TypeScript errors in AskAssistant, QuickBooking, and assistantRetrieval. | Fixed dataset access, React children typing, intent narrowing, and undefined-safe string handling; reran `bunx tsc --noEmit`.             | Static typecheck now passes; keep the focused fixes in the Phase 2 quality history. | Codex / repository owner                  | Resolved             |
| B-005 | Administrator notification dispatch still needs a sender credential. | Live Edge Function Secrets for `jkhxtuwqmmdetjqymzso` show only default reserved secrets and **No custom secrets created**. Per D-018/D-024/D-026, do **not** create a paid Resend account first. GitHub Student Pack SMTP/email is the first source, provisioned in **Phase 8**. Phase 4 still needs real-delivery evidence once a sender exists. Owner login/OTP is required to claim Student Pack and paste an SMTP/API secret into Edge Function Secrets. | Outbox rows, retry UI, and live function dispatch are ready. Email delivery stays Blocked until Student Pack (or an approved fallback after Student Pack cannot provide SMTP) is configured. Not a Phase 2 fail. | Repository owner | Open |

## Update protocol

- Set a task to `In progress` before changing its scope; set it to `Complete` only with concrete evidence.
- Use `Blocked` only when the work cannot proceed without an approved decision or external change; create a corresponding decision blocker.
- Do not remove completed tasks or blockers; preserve history and add a new row if the work is reopened.
- The agent should resolve ordinary technical questions from the repository and current decisions. Only decision blockers require a recorded escalation.

## Must-solve by phase (do not start later items now)

See `DEVELOPMENT_PLAN.md` and D-021–D-028. Current slice is News / published-content J Agent / specialist deep links. Asana is the visual board only (D-028).

| Must solve | Phase | Decision |
| --- | --- | --- |
| Dual-field bilingual; empty EN → Chinese fallback | Keep now | D-021 |
| Chinese-only Admin + EN auto-translate adapter | 6 | D-021 |
| J Agent retrieval from published content | 2/3 | D-019, D-022 |
| Optional vector/provider RAG | 6 | D-022 |
| Specialist deep links + Admin URLs + enquiry | 3 / current slice | D-020, D-023 |
| Messenger **and** LINE OA webhooks | 7 | D-003, D-023 |
| Student Pack email/domain/monitoring; no production promote | 8 | D-018, D-024 |
| notify-admin real delivery evidence | 4 | D-026 |
| Remaining hover second frames | Post-launch | D-025 |
| PR #4/#5 onto `main`, DNS/SSL, Sentry, backup | 8/9 | D-027 |

## Project governance

| ID      | Task                                                                 | Owner   | Status      | Dependency | Evidence / next action                                                                                                                                                                                                                                                                                                                                 |
| ------- | -------------------------------------------------------------------- | ------- | ----------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| GOV-001 | Reconcile control records with the current branch and pull requests. | Manager | Complete | None       | Reconciled RLS/public-media, password-policy, Bun, PR-chain, current-commit, and evidence-classification conflicts. Verified from `c84ee24` on `feature/release-governance`: `bunx tsc --noEmit`, `bun run lint` (0 errors, 9 warnings), `bun run build`, `git diff --check`, and `git diff --check origin/main` exit 0. PR #5 is an 84-commit draft into the still-open PR #4 branch; neither has reviewer approval. Authenticated external-platform claims were not independently reverified. |
| GOV-002 | Restore enforceable pull-request and main-branch quality gates.       | Manager | Complete | GOV-001    | Opened governance [PR #6](https://github.com/veryjades/Asian-Models/pull/6) into `feature/mock-assets` and focused CI [PR #7](https://github.com/veryjades/Asian-Models/pull/7) into `feature/phase-1-foundation`; both pinned-Bun CI runs passed. [PR #7](https://github.com/veryjades/Asian-Models/pull/7) is merged. Corrected PR #5's title/body without changing its base or history. Enabled `main` protection with strict `Typecheck, lint, and build`, one approval, stale-review dismissal, conversation resolution, admin enforcement, and force-push/deletion denial. Self-approval of own PRs is blocked by GitHub; [PR #6](https://github.com/veryjades/Asian-Models/pull/6) merged into `feature/mock-assets` as `2db6fd6` without targeting `main`. |
| GOV-003 | Reverify external release gates and restore the published About boundary. | Manager | Complete | GOV-002, D-015 | Live SQL/REST: 13 active models, 3 published News, 0 public media objects. Asana 模特經紀網站發佈計畫 is the owner visual board only (D-028); synced 2026-08-15 to Phase 8 / Phase 6 / Now / Phase 7 / Post-launch without a duplicate project. About Admin→public marked complete with GATE-20260815-2248. The old 2/17 (12%) snapshot is not engineering truth. Edge Function Secrets: no custom secrets (B-005 remains open). Published-About anon column-read returns HTTP 200; `admin_email` / `select=*` remain 401. CMS-002 About Admin→public verified. [PR #6](https://github.com/veryjades/Asian-Models/pull/6) merged into `feature/mock-assets` as `2db6fd6`. Remaining second-pose photos are post-launch (D-025). |

## Phase 1 — Architecture Foundation

| ID     | Planned task                                                                                  | Owner | Status   | Dependency | Next step                                                                                                                                                                                 |
| ------ | --------------------------------------------------------------------------------------------- | ----- | -------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1-001 | Start the approved architecture foundation.                                                   | Codex | Complete | D-007      | ADR-001 records the approved scope and boundaries.                                                                                                                                        |
| P1-002 | Create Supabase local configuration, environment template, and typed browser client boundary. | Codex | Complete | P1-001     | `supabase/`, `.env.example`, `src/lib/supabase/client.ts`, and the client package are present without secrets or UI wiring.                                                               |
| P1-003 | Create initial PostgreSQL migration, database types, and agency adapter layer.                | Codex | Complete | P1-001     | Migration `20260810150546_initial_platform_foundation.sql` defines models, portfolios, media assets, clients, bookings, indexes, timestamps, and RLS.                                     |
| P1-004 | Record the first-pass RLS policy plan.                                                        | Codex | Complete | P1-003     | `docs/RLS_POLICY_PLAN.md` defines the role/access matrix; no browser policies or grants are enabled.                                                                                      |
| P1-005 | Apply and validate the migration against a local or linked Supabase project.                  | Codex | Complete | D-011      | Historical application to `cajkkxustehtzyymlopm` is retained for audit; the current live project is `jkhxtuwqmmdetjqymzso`, where the same foundation was re-applied and verified through SQL Editor on 2026-08-15. |

Phase 1 architecture foundation is complete. No existing UI route is connected to Supabase yet; Auth, Storage buckets, browser grants, and Admin/Editor/Viewer enforcement are Phase 2 work.

## Phase 2 — Application Foundation

| ID     | Planned task                                                                 | Owner | Status   | Dependency | Next step                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------ | ---------------------------------------------------------------------------- | ----- | -------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P2-001 | Apply the approved schema and refresh the typed database boundary.           | Codex | Complete | D-011, D-014 | Foundation, security, and admin-media migrations are applied to `jkhxtuwqmmdetjqymzso`; live verification returned the expected tables, private bucket, and zero seed rows. |
| P2-002 | Define concrete Auth, Storage, and RLS policy tests for Admin/Editor/Viewer. | Codex | Complete | P2-001     | Phase 2 security migration applied; private `model-media` bucket, trusted `app_metadata.role` function, grants, policies, advisor check, viewer-deny probe, and editor-allow probe are recorded in `docs/RLS_POLICY_PLAN.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| P2-003 | Implement Auth, Storage boundaries, and role enforcement.                    | Codex | Complete | P2-002     | Added browser-safe Auth (`src/lib/supabase/auth.ts`), private Storage (`src/lib/supabase/media.ts`), and deployed server-only `provision-user` Edge Function v2. The live function uses custom `auth.getUser()` plus an Admin `app_metadata.role` check; legacy gateway JWT verification is disabled so browser CORS preflight can reach the function. Live evidence on 2026-08-15: three disposable viewer/editor/admin sessions with refreshed claims; database matrix passed viewer reads, editor insert/update, editor delete denial, and admin delete success across models/portfolios/media/clients/bookings. Storage matrix passed viewer download/upload denial, editor upload/update/delete denial, admin delete, and anonymous private-object denial. All probe rows/objects were cleaned. Supabase Auth policy is project-configurable; the owner-authorized Email provider setting is currently minimum 6 characters with no required character classes. Leaked-password protection remains Pro+ only. |

## Admin UI preparation — owner-authorized bounded slice

| ID      | Task                                                                                                 | Owner | Status   | Dependency       | Evidence / next action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------- | ---------------------------------------------------------------------------------------------------- | ----- | -------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ADM-001 | Build the authenticated model media admin surface for primary + hover uploads.                       | Codex | Complete | D-012, P2-003    | Added `/admin` with Auth sign-in, Admin/Editor gate, model create/update form, primary photo slot, optional hover photo slot, private Storage uploads, and `media_assets.sort_order` 0/1 persistence. `bunx tsc --noEmit`, `bun run lint`, and `bun run build` pass; local browser QA at `/admin` showed the configured sign-in state, no console errors, and no horizontal overflow.                                                                                                                                                                                                                                                                                                                       |
| ADM-002 | Extend each model profile with unlimited gallery media, MP4 upload, YouTube links, and social links. | Codex | Complete | D-013, ADM-001   | Migration `20260814190731_admin_media_links.sql` is applied to current live project `jkhxtuwqmmdetjqymzso`; `/admin` supports multi-file gallery selection, MP4 validation, YouTube URL validation, HTTPS social links, and Admin-only deletion. |
| ADM-003 | Make the Admin first-time setup and password recovery path deliverable.                              | Codex | Complete | P2-003, ADM-001  | Added reset/update methods to the browser-safe Auth adapter and a `/admin` password setup screen that delegates minimum length and character rules to Supabase Auth. Expired/invalid invite or recovery hashes are routed to `/admin?reset_error=...` with a fresh-link action; successful updates sign out explicitly so the new password is tested on the next sign-in. Supabase Auth URL Configuration uses `http://127.0.0.1:8090` as Site URL and allows local/Preview wildcards; Dashboard verification reported `Successfully added 3 URLs`. |
| ADM-004 | Bootstrap the owner Admin account and issue a fresh password setup link.                             | Codex | Complete | ADM-003, P2-003  | In current project `jkhxtuwqmmdetjqymzso`, `menscheck@gmail.com` has a live invitation row and trusted server-side `app_metadata.role=admin`. Auth Logs verified `/invite` HTTP 200 followed by `/verify` HTTP 303; a later recovery request also returned `/recover` HTTP 200. No password is stored in the repository or browser code. |
| ADM-005 | Align the live password policy and verify invite/recovery deep-link handling.                        | Codex | Complete | ADM-003, ADM-004 | Owner-authorized Supabase Email provider setting is saved at minimum 6 characters with no required character classes, making the requested six-digit password policy-compatible. Local browser QA navigated to a homepage invite hash and observed automatic redirect to `/admin?reset=1` with the auth hash preserved; expired-link routing now exposes a recovery action and the setup form listens for `PASSWORD_RECOVERY`.                                                                                                                                                                                                                                                                                                                        |
| ADM-006 | Re-send the owner password-reset invitation after the provider rate window clears.                   | Codex | Blocked  | ADM-004, ADM-005 | A fresh recovery request on current project `jkhxtuwqmmdetjqymzso` returned `/recover` HTTP 200 and then `/verify` HTTP 303. A subsequent duplicate request returned `/recover` HTTP 429 (`email rate limit exceeded`), confirming Supabase delivery protection after the successful send. The existing setup path is ready; retry only after the provider window clears or a configured SMTP provider is available. |
| ADM-007 | Implement invitation-first Admin/Editor/Viewer access management.                                   | Codex | Complete | D-017, P2-003 | Admin-only `/admin` Access / invitations panel is live. The deployed `provision-user` function validates the caller with `auth.getUser()` and trusted `app_metadata.role=admin`, sends an invite to the approved `/admin?reset=1` route, assigns the selected role, lists confirmed/pending users, supports role changes, and guards self-deletion. CORS preflight and the custom auth path were verified against the live project; the owner account appeared in the panel. |

## Content and JAgent expansion — owner-authorized checklist

| ID | Task | Owner | Status | Evidence / next action |
| --- | --- | --- | --- | --- |
| CMS-001 | Seed the existing 13-model roster into the current Supabase project. | Codex | Complete | Live SQL verification on 2026-08-15 returned `model_count=13` in project `jkhxtuwqmmdetjqymzso`. |
| CMS-000 | Prove the Admin → Supabase → public-route data path for every content type. | Codex | In progress | **2026-08-16:** Tag pages used seed roster + exact slug match, so a new woman could appear on `/models/women` but not `/keywords/editorial-model` (Admin `editorial` ≠ slug `editorial-model`). Fixed: keyword results use live `listModels()`, canonicalize aliases, Admin checkbox taxonomy, React Query invalidation after model/YouTube/About/News save. YouTube URLs canonicalize before insert. Unlimited gallery photos/MP4 with 10/50 MiB limits (D-030). Chinese-first Admin + empty EN → ZH fallback (D-029). Public slots D-031. Hover extras remain D-025. Cover file-upload UI still URL-only. |
| CMS-004 | Connect published model media, video, and social rows to public profiles/cards. | Codex | Complete | `20260815210000_public_content_grants.sql` applied in project `jkhxtuwqmmdetjqymzso`; `src/lib/content/repository.ts` reads `media_assets`, `model_video_links`, and `model_social_links`, filters published visibility, maps primary/hover sort order, and exposes public Storage URLs. Live REST probes returned HTTP 200 for all four public tables; media/link row counts are currently zero. |
| CMS-005 | Backfill Traditional Chinese model names and cities for the existing roster. | Codex | Complete | SQL Editor execution on 2026-08-15 updated all 13 active model rows, including `bio_en` and `bio_zh`. Live REST verification returned non-null `name_zh`, `city_zh`, `bio_en`, and `bio_zh` for every slug; public UI keeps bilingual `pick()` fallback and Admin exposes both language fields. |
| CMS-002 | Add About page content management and published public binding. | Manager | Complete | Owner Admin JWT on local `http://127.0.0.1:8090/admin` (`menscheck@gmail.com`, `app_metadata.role=admin`, `getUser()` ok, localStorage token present). UI Save of `About GATE-20260815-2248` POSTed `site_settings` HTTP 200 with a Bearer user JWT. Publishable-key REST returned the same title; English `/about` h1 rendered `About GATE-20260815-2248`. Title restored to `About` (REST 200). Public heading now binds CMS titles. Viewer/Editor deny remains unverified. Preview `feature/admin-auth-jwt` `/admin` was signed out during this pass. |
| CMS-003 | Add News CRUD, media, publish state, tags, and public route binding. | Codex | In progress | Schema/RLS, publish/list/delete, seed rows, public `/news` binding, Admin Edit, and React Query invalidation after save are in code. Unique-slug publish `gate-20260815-2312` previously reached public `/news` then restored. **2026-08-16:** same-row Admin→public refresh was not re-run; local `/admin` and Preview `/admin` were signed out. Cover file-upload UI remains. |
| JAG-001 | Overlay-only knowledge-document management for uncovered special questions. | Codex | In progress | D-019/D-022: not the default corpus. Schema/RLS, Admin create/list/delete, published-only overlay retrieval exist. Do not treat “add a doc then retrieve” as the product gate. |
| JAG-002 | Connect JAgent to published model/news/About retrieval, with specialist handoff. | Codex | In progress | D-019/D-020/D-022/D-023. **2026-08-16 local `127.0.0.1:8091`:** `我要找男模` returned Lin Wei-Jie, Noah Castellanos, Han Min-jae, Kim Do-yun. `請幫我轉接專人` showed enquiry + Quick Booking; Messenger/LINE placeholders because Admin URLs are empty. Live SQL confirmed `messenger_url` and `line_oa_url` on `site_settings`. Vector/provider RAG is Phase 6. Webhooks are Phase 7. |
| OPS-001 | Persist client enquiries and model applications in an Admin inbox. | Codex | Complete | Contact, Quick Booking, and scouting forms write Supabase rows; `/admin` lists and status-updates both queues. |
| OPS-002 | Enqueue and dispatch administrator email notifications. | Codex | In progress | D-026: real-delivery evidence is Phase 4; Student Pack SMTP as production sender is Phase 8. Outbox + `notify-admin` exist; B-005 remains Open. |
| REL-001 | Complete the master checklist QA, Preview verification, and PR #5 readiness review. | Codex | In progress | D-027: PR #4/#5 onto `main` is Phase 8/9, not now. Do not retarget PR #5 to `main` until PR #4 is reviewed/merged. |

## Experience-validation iteration — Mock assets

| ID     | Task                                                                                                                     | Owner | Status   | Dependency            | Evidence / next action                                                                                                                                                                                                                                              |
| ------ | ------------------------------------------------------------------------------------------------------------------------ | ----- | -------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| UX-001 | Record the fictional, licensed/AI-first mock asset policy and future CMS replacement path.                               | Codex | Complete | D-009                 | `docs/MOCK_ASSET_GUIDE.md` defines source policy, review checks, prompts, provenance metadata, dimensions, and replacement workflow.                                                                                                                                |
| UX-002 | Add a presentation component that accepts asset-level aspect ratio, focal position, fit, and responsive source metadata. | Codex | Complete | UX-001                | `AgencyImage` is applied to hero, portrait cards, full-body profile, and gallery surfaces. `bun run lint` passes with 0 errors; `bun run build` passes. Asset-specific visual validation remains UX-004.                                                            |
| UX-003 | Generate and review fictional premium-fashion mock assets.                                                               | Codex | Complete | Approved CLI fallback | 18 reviewed fictional adult WebP assets are mapped in `src/lib/content/mockAssets.ts`; provenance, prompt intent, dimensions, and usage are recorded in `docs/GOLDEN_MOCK_ASSET_SET.md`.                                                                            |
| UX-004 | Deploy the resulting branch to an isolated Preview and complete desktop/mobile experience review.                        | Codex | Complete | UX-002, UX-003        | Preview `asian-models-gxon1hn2t-asian-models.vercel.app` is READY for commit `02ab7ea`; desktop and 390px DOM review passed for the three hero CTAs, board/profile mapping, and desktop arrows. Touch swipe is implemented but needs stakeholder device acceptance. |

| UX-005 | Make the homepage image sequence visually consistent from Hero through Featured Models and News. | Codex | Complete | UX-004, D-008, D-009 | Replaced News covers with the reviewed Golden `portfolio.womenEditorial`, `portfolio.menCommercial`, and `portfolio.beautyCampaign` assets. Homepage plus News surfaces now use `AgencyImage` with a 3:2, non-cropping frame. `bun run lint` passed with 0 errors (9 existing warnings); `bun run build` passed; Preview `asian-models-p2kd962ta-asian-models.vercel.app` passed desktop and 390px review with no console errors. |

| UX-006 | Recover missed public-experience requirements for favicon, keyword discovery, booking CTA reliability, and Golden model identity. | Codex | Complete | UX-005, D-008, D-009 | Added the J_J SVG favicon; introduced an extensible keyword/tag content boundary consumed by the homepage Keyword Dynamic Runway; added model/news tags and keyword result pages that route to relevant models, portfolio signals, news, and Quick Booking; documented future admin/CMS keyword management in `docs/KEYWORD_TAG_SYSTEM.md`; removed cross-person generic portfolio images from Golden model profile galleries. `bun run lint` passed with 0 errors (9 existing warnings); `bun run build` passed; local browser verification passed for desktop, 390px, keyword result, model-specific booking modal, recruitment photo gate, and console errors. Preview validation remains the final evidence after deployment. |

| UX-007 | Final UX quality-gate recovery for profile booking layering, header/profile CTA clarity, mobile keyword runway usability, and model-profile identity depth. | Codex | Blocked | UX-006, D-008, D-009 | Implemented a top-level Quick Booking portal overlay with scroll lock and inert background; hid the generic header booking CTA on model profile pages so the profile has one model-specific Quick Booking action; improved the 390px Keyword Dynamic Runway so active keywords do not visually collapse; verified recruitment photo gate behavior locally. Remaining blocker: audited Golden model profiles still do not have four same-person photos per model, and this environment does not expose a same-identity image generation tool or `OPENAI_API_KEY`; do not fake compliance by duplicating assets or mixing identities. |

| UX-008 | Correct the Keyword Dynamic Runway from a one-direction ticker into a bidirectional shallow-depth runway. | Codex | Complete | UX-006, D-008, D-009 | Updated the homepage runway to a 40px full-width strip directly below Hero; kept the top 15 active canonical keywords as the data source; changed the animation to LEFT↔RIGHT with C-position focus, depth z-order, fade/blur after passing C, controlled overlap, and desktop hover/focus override. Local browser verification passed for desktop and 390px: no horizontal overflow, no second line, keyword click to `/keywords/editorial-model` showed Models/Portfolio/News/Quick Booking, Hero/Featured/News composition regressed cleanly, Quick Booking overlay and recruitment 2-photo gate remained valid. `bun run lint` passed with 0 errors and 9 existing warnings; `bun run build` passed. Preview validation follows after push. |
| UX-009 | Correct Keyword Dynamic Runway focus interaction so hover and automatic C-state preserve spatial position and scale only. | Codex | Complete | UX-008, D-008, D-009 | **Problem:** hover previously forced a keyword to the C position and inverted its colours. **Assumption:** separate the animated runway transform from a nested text-scale transform. **Smallest change:** animate only the label scale through five visual levels; on hover/focus pause the runway at its current position and scale the existing label to 2.4× with a foreground z-order, transparent background, and unchanged text colour. **Preview evidence:** Desktop Chrome pointer test on `d136c69` showed `:hover`, frozen non-zero runway transform, 2.4× label scale, z-index 20, transparent background, unchanged text colour, and no horizontal overflow. The automatic C-state stayed in place at 2.4×. At 390px, the runway remained 40px high with 15 keywords, one line, no horizontal overflow, and no console errors. No MP4 file was included in the accessible attachment directory. |
| UX-010 | Complete identity-consistent paired model-card photography, profile completeness, and model-profile booking placement. | Codex | In progress | D-025: remaining hover second frames are **post-launch Admin uploads**, not a Phase 2 blocker. Do not generate substitutes. Existing reviewed pairs stay; cards without a same-person second frame show primary only. | **Problem:** profile booking was in content flow, profile statistics lacked weight, and category/tag/discovery cards had no valid same-session hover frame. **Implemented:** all 13 fictional profiles now have a weight; names, Chinese renderings, city context, and bio references were audited; model-specific Quick Booking is fixed at viewport bottom-left; `ModelCard` supports a white shutter flash followed by only `hoverPortrait` second-frame media on category, keyword, and homepage discovery cards. **Incremental evidence:** Chen Yu-Xin, Aoi Takahashi, and Lin Wei-Jie have reviewed same-person editorial pairs. Tanya Lim and Han Min-jae retain same-person full-body extensions as second frames while stronger pose variants are sourced. The current execution pass removed the remaining Hina/Kim cross-person hover mappings and their mixed-identity gallery entries; those cards now safely render only their primary digital until genuine same-person frames are available. An audit removed Lin's unrelated beach image and cross-person legacy gallery images from six talent profiles. Profile gallery media now uses a clipped `cover` frame to prevent letterboxing. A fresh seed audit found no media path shared across different model slugs; the only black-edge legacy asset (`src/assets/model-03.jpg`) is unused. **Required next evidence:** remaining second frames are post-launch Admin uploads (D-025), not a Phase 2 gate. |
| UX-011 | Replace J Assistant's browse-yourself answers with structured roster retrieval. | Codex | Complete | UX-006 | `assistantRetrieval.ts` now ranks repository-backed candidates from reviewed model/profile language, keyword/tag, portfolio, and News data; J Assistant renders direct profile and model-specific Quick Booking actions. `docs/ASSISTANT_RETRIEVAL.md` records the cloud-agnostic boundary and future Supabase/pgvector handoff. The five required Chinese example queries were executed locally and returned concrete candidates. |
| UX-012 | Prevent J Assistant from being covered by Vercel Preview Toolbar. | Codex | Complete | UX-011 | Moved the J Assistant trigger clear of the Preview Toolbar and raised its interaction layer; once open, it hides the Preview Toolbar for the duration of the conversation. Live Preview click verification on `fe413cb` opened J Assistant rather than the toolbar and returned four candidates for `我要找男模`; 390px passed without horizontal overflow or console errors. |

### UX-002 iteration card

1. **User-experience problem:** current prototype imagery can read as a placeholder and wide hero framing can crop a model's head or feet.
2. **Assumption:** asset-level presentation metadata and a safe rendering boundary prevent pages from silently imposing an unsafe crop.
3. **Smallest verifiable change:** one reusable `AgencyImage` component plus opt-in use on hero, portrait-card, full-body, and portfolio surfaces.
4. **Preview validation:** inspect the homepage, board, and model profile at desktop and 390px; verify intended focal areas and no crop imposed by page CSS.
5. **Next observation:** determine which generated asset composition requires a separate mobile source rather than a focal-point adjustment.
